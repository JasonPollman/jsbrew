/**
 * Exports the `Null` function.
 * @since 9/25/18
 * @file
 */

/**
 * A function that always returns `null`.
 * @returns {boolean} The liternal `null`.
 * @export
 */
export default function Null() {
  return null;
}
