/**
 * Exports the `identity` function.
 * @since 9/25/18
 * @file
 */

/**
 * A function that always returns the first argument provided to it.
 * @param {any} x Input.
 * @returns {any} The value if the first argument.
 * @export
 */
export default function identity(x) {
  return x;
}
