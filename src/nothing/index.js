/**
 * Exports the `nothing` function.
 * @since 9/25/18
 * @file
 */

/**
 * A function that always returns `undefined`.
 * Some libraries refer to this as a `noop` function.
 * @returns {undefined} The liternal `undefined`.
 * @export
 */
export default function nothing() {}
