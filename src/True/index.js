/**
 * Exports the `True` function.
 * @since 9/25/18
 * @file
 */

/**
 * A function that always returns `true`.
 * @returns {boolean} The liternal `true`.
 * @export
 */
export default function True() {
  return true;
}
